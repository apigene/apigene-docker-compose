# Minimal logging for local Docker (errors still go to the container; not forwarded to `docker compose logs`)
storage:
  dbPath: /data/db

systemLog:
  destination: file
  path: /dev/null
  quiet: true

net:
  bindIpAll: true
